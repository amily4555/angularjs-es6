/**
 * be-footer
 * angular component demo
 */
import template from './be-footer.html';
import controller from './be-footer.ctrl';

let beFooterCmp = {
    bindings: {
    },
    transclude: true,
    template,
    controller
};

export default beFooterCmp;

